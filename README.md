# 🕶️ АНОНИМ БОТ

Telegram бот для анонимных вопросов. Пользователь переходит по уникальной ссылке и отправляет анонимный вопрос нужному админу. Админ может ответить пользователю — ответ придёт от имени бота, личность останется скрытой.

---

## ⚙️ Функционал

**Для пользователя:**
- Переходит по ссылке `?start=alice` или `?start=mac`
- Пишет анонимный вопрос
- Получает кнопку "Отправить ещё ✉️" после отправки
- Может получить анонимный ответ от бота
- Антиспам — одно сообщение раз в 30 секунд

**Для админов:**
- Получают сообщения в формате `👀 Кто отправил / ✏️ Что написал`
- Под каждым сообщением кнопка "Ответить 💬"
- Команда `/stats` — статистика сообщений за сегодня

**Особенности:**
- Главный наблюдатель (OiBoi) видит ВСЕ сообщения всех админов незаметно для них
- Все данные сохраняются в `data/storage.json` — при перезапуске ничего не теряется
- Все сообщения логируются в `logs/messages.txt`

---

## 📁 Структура файлов

```
anontg/
├── bot/
│   ├── bot.py            # запуск бота и регистрация хендлеров
│   ├── config.py         # токен, ID админов, настройки
│   ├── handlers.py       # вся логика бота
│   ├── storage.py        # работа с данными
│   ├── requirements.txt  # зависимости
├── data/
│   └── storage.json      # база данных бота
└── logs/
    └── messages.txt      # лог всех сообщений
```

---

## 🚀 Установка и запуск

### 1. Клонируй репозиторий
```bash
git clone https://github.com/твой_ник/anontg.git
cd anontg
```

### 2. Установи зависимости
```bash
pip install -r bot/requirements.txt
```

### 3. Вставь токен бота
Открой `bot/config.py` и вставь токен который получил от @BotFather:
```python
TOKEN = "сюда_вставь_токен"
```

### 4. Запусти бота
```bash
python bot/bot.py
```

---

## 🔗 Ссылки для админов

| Админ | Ссылка |
|-------|--------|
| Алиса (`@aliswesh`) | `t.me/имя_бота?start=alice` |
| Мацукевич (`@masukav1ch`) | `t.me/имя_бота?start=mac` |

---

## 🛠️ Стек

- Python 3.10+
- [python-telegram-bot 20.7](https://github.com/python-telegram-bot/python-telegram-bot)
